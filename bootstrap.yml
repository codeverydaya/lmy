eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: invest
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
        
